# file-exchange-server-java
A multi-threaded client-server file exchange system implemented in Java using Socket programming. The application supports concurrent user connections, unique handle registration, and synchronized file operations including uploading, downloading, and remote directory listing, all managed through a terminal interface.
